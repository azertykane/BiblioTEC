version: '3.9'

services:
  web:
    build: .
    container_name: bibliotheque_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: gunicorn bibliotheque.wsgi:application --bind 0.0.0.0:8000
