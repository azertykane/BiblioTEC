apiVersion: v1
kind: Secret
metadata:
  name: bibliotec-secrets
  namespace: bibliotec
type: Opaque
stringData:
  # Django secret key (CHANGER EN PRODUCTION)
  DJANGO_SECRET_KEY: "django-insecure-qlgkgnoi6vl_#1yq=mymy#b9f4t)ijp*2o1p8o9m*9fe5tg180"
  
  # PostgreSQL credentials
  POSTGRES_USER: "bibliotec_user"
  POSTGRES_PASSWORD: "changeme123"
  
  # Database URL
  DATABASE_URL: "postgresql://bibliotec_user:changeme123@postgres-service:5432/bibliotec_db"
  
  # Email credentials (optionnel)
  EMAIL_HOST_USER: "your-email@example.com"
  EMAIL_HOST_PASSWORD: "your-password"

